# TTS 제작 가이드

## 파일 버전 관리

- TTS, 이미지, 영상 등 모든 반복 생성물은 버전마다 별도 파일명으로 저장한다.
- 파일명은 `-v1`, `-v2`, `-v3` 또는 용도에 따라 `-raw`, `-edited`, `-final`을 사용한다.
- 기존 생성물을 자동으로 덮어쓰지 않는다.
- 최종 확정본은 사용자 승인 후 `-final` 파일로 별도 저장한다.
- 이전 버전은 사용자의 삭제 승인 전까지 보존한다.

## TTS 생성 및 교정

- 느낌표(`!`)는 1초 이상의 과도한 pause를 유발할 수 있다. 가능하면 마침표나 자연스러운 어감으로 대체하고, 느낌표가 꼭 필요하면 생성 후 무음 트림을 전제로 한다.
- 쉼표 기준으로 문장을 나눠 각각 생성하면 결과가 예측 불가능하다. 자연스러워질 수도 있지만 호출 사이에 목소리 톤이 달라질 수 있다. 먼저 통문장으로 생성하고, 문제가 확인된 경우에만 분리 생성을 시도한다.
- 쉼표의 pause가 불안정하면 마침표로 문장을 나눠 생성하는 방법을 시도할 수 있다. Pause는 개선될 수 있지만 두 호출 사이의 톤 차이가 생길 수 있으므로, 필요하면 문제 구간만 재생성해 기존 음성에 부분 교체한다. 전체 음성을 불필요하게 재생성하지 않는다.
- `speakingRate`는 0.05 단위로 세밀하게 조정한다. 큰 폭으로 낮추면 발음이 어눌해질 수 있다.
- 무음 문제는 먼저 `ffmpeg`의 `silencedetect=noise=-30dB:d=0.1` 또는 `d=0.15`로 진단한다. 무음 트림만으로 해결할 수 있으면 TTS를 재생성하지 않는다.
- 원본 음성과 편집본은 각각 `-raw`와 `-edited` 또는 `-final`로 분리 보관한다.
